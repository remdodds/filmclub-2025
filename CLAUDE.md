# Film Club 2025

## Testing

After every code change, run the backend unit tests and ensure they all pass:

```bash
cd functions && npm install && node_modules/.bin/jest
```

If you add new functionality, write tests for it in the corresponding `*.test.ts` file before considering the task complete.
